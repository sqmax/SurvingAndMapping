<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.survingandmapping">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/iconce"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/iconce"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".Suizhun"
            android:label="水准测量">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".SuizhunResult" />
        <activity
            android:name=".DaoXianActivity"
            android:label="导线测量" />
        <activity
            android:name=".DaoXianBegin"
            android:label="导线测量，平差"></activity>
        <activity
            android:name=".PingchaResult"
            android:label="平差结果"></activity>
    </application>

</manifest>